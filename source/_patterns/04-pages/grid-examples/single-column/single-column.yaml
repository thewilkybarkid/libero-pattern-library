content:
  - template: organisms-content-header
    arguments:
      contentTitle:
        text: Content title
      categories:
        attributes:
          aria-label: Category listings that include this article
        items:
          - content:
              attributes:
                href: '#'
              text: Cell Biology
          - content:
              text: Epidemiology and Global Health
  - template: templates-single-column
    arguments:
      content: single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column single column
  - template: templates-full-width
    arguments:
      content: full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full widthfull width full width full width full width full width full width full width full width full width full width full width full width full width full width full width full width
  - template: templates-tertiary-column
    arguments:
      content: tertiary column tertiary column tertiary column tertiary column tertiary column tertiary column tertiary column tertiary column tertiary column tertiary column tertiary column tertiary column
  - template: templates-primary-column
    arguments:
      content: primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column primary column
  - template: templates-secondary-column
    arguments:
      content: secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column secondary column
